name: zoom_hover_pinch_image
description: "This is an image zoom package, which you can easily use."
version: 1.0.1
homepage: "https://github.com/SwanFlutter/zoom_hover_pinch_image.git"

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0


flutter:

