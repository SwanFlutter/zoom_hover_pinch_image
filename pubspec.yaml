name: zoom_hover_pinch_image
description: "An image zoom package with easy integration, pinch-to-zoom interaction, and customizable border radius."
version: 1.0.4
homepage: "https://github.com/SwanFlutter/zoom_hover_pinch_image.git"

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0


flutter:

